git init # Initializes an empty git
git status # displays information about state of git
git add <filename> # adds file to the git repository
### ^^^ this adds files to the staging area
git diff <filename> # checks the differences for a file between working directory and staging area
git commit -m "Message when committing"
git log # allows us to see a log of all our versions and commits